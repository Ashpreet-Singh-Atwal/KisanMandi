<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>MSP Rates - KisanMandi</title>

<style>
    body {
        margin: 0;
        font-family: "Poppins", sans-serif;
        background: #f4f7fb;
    }
    .header {
        background: #3498db;
        padding: 20px 30px;
        color: white;
        font-size: 26px;
        font-weight: 600;
    }
    .container {
        width: 90%;
        margin: 25px auto;
    }
    .back-btn {
        display: inline-block;
        background: #2980b9;
        color: white;
        padding: 10px 20px;
        border-radius: 8px;
        font-weight: 500;
        text-decoration: none;
        margin-bottom: 20px;
    }
    .msp-card {
        background: white;
        padding: 18px 22px;
        border-radius: 12px;
        margin-bottom: 18px;
        box-shadow: 0 6px 15px rgba(0,0,0,0.1);
        display: flex;
        justify-content: space-between;
        align-items: center;
        transition: 0.2s ease;
    }
    .msp-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 18px rgba(0,0,0,0.12);
    }
    .crop-name {
        font-size: 20px;
        font-weight: 600;
        color: #2c3e50;
    }
    .crop-msp {
        font-size: 18px;
        font-weight: 600;
        color: #3498db;
    }
</style>
</head>

<body>

    <div class="header">📘 Minimum Support Prices (MSP)</div>

    <div class="container">

        <a href="/farmer/dashboard" class="back-btn">⬅ Back to Dashboard</a>

        <% if (mspList && mspList.length > 0) { %>

            <% mspList.forEach(item => { %>
                <div class="msp-card">
                    <div class="crop-name"><%= item.crop %></div>
                    <div class="crop-msp">₹ <%= item.msp %> / quintal</div>
                </div>
            <% }) %>

        <% } else { %>
            <p style="color:#7f8c8d;font-size:18px;">MSP data not available.</p>
        <% } %>

    </div>

</body>
</html>
